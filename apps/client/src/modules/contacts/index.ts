/** The module's only public entry point — every other layer is private. */
export { ContactsScreen } from "./presentation/main"
export { contactsSearchSchema } from "./domain/view-state"
export type { Contact } from "./domain/models"
